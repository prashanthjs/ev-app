# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build ev-api1`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t ev-api1`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system ev-api1 && \
          adduser --system -G ev-api1 ev-api1

COPY dist/apps/ev-api1 ev-api1/
RUN chown -R ev-api1:ev-api1 .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix ev-api1 --omit=dev -f install

CMD [ "node", "ev-api1" ]
